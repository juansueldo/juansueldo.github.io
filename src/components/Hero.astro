---
import HiglightButton from "./HiglightButton.astro";
import Hexagon from './Hexagon.astro';
import GitHubIcon from './icons/Github.astro';
import LinkedInIcon from './icons/Linkedin.astro';
import Links from './Links.astro';
interface Props {
  hello: string,
  name: string
  hightlight_text?: string,
  role: string
  description: string
  projectsLabel: string
  projectsUrl: string
  cvLabel: string
  cvUrl: string
  githubLabel: string
  githubUrl: string
  linkedinLabel: string
  linkedinUrl: string
  hexagon: {
    ariaLabel: string
    items: Array<{
      type: string
      label: string
      value?: string
      suffix?: string
    }>
  }
}

const {
  hello, name, hightlight_text, role, description,
  projectsLabel, projectsUrl, cvLabel, cvUrl,
  githubLabel, githubUrl, linkedinLabel, linkedinUrl, hexagon
}: Props = Astro.props
---
<section id="hero" class="relative flex min-h-screen w-full items-center justify-center overflow-hidden px-4 text-center">
    <div class="absolute inset-0 -z-10 bg-[url('/bgLight.svg')] dark:bg-[url('/bgDark.svg')] bg-repeat bg-[length:500px]"></div>
    <div class="absolute inset-0 -z-10 pointer-events-none bg-gradient-to-b from-[rgba(255,255,255,0.3)] via-transparent to-[#f8f7fa] dark:from-[rgba(17,17,17,0.8)] dark:via-transparent dark:to-[#111]"></div>
    <div id="light" class="absolute w-40 h-20 -z-50  blur-[15px] opacity-100 bg-gradient-to-r from-blue-400 via-[#0af019] to-[#0088ff] rounded-full pointer-events-none transition-transform duration-300 ease-linear" style="transform: translate(-50%, -50%)"></div>
    <div class="mx-auto grid w-full min-w-0 max-w-6xl items-center gap-10 py-24 sm:px-5 lg:grid-cols-[.9fr_1.1fr] lg:px-8">
      <div class="relative z-10 min-w-0 text-center lg:text-left">
        <h1 class="reveal-left break-words text-[clamp(3rem,15vw,5.5rem)] font-bold leading-[1.05] hightlight-text">{hello}</h1>
        <h2 class="reveal-left mt-3 break-words text-[clamp(3rem,15vw,5.5rem)] font-bold leading-[1.05] text-black dark:text-white">{name}</h2>
        <HiglightButton width="50%" id="hero-button" text={role} hightlight_text={hightlight_text} url={'#experience'}/>
        <p class="mx-auto mt-5 max-w-xl text-sm leading-relaxed text-slate-600 dark:text-slate-300 sm:text-base lg:mx-0 lg:text-lg">{description}</p>
        <div class="mt-8 flex flex-col justify-center gap-3 sm:flex-row lg:justify-start">
          <a href={projectsUrl} class="rounded-md px-6 py-3 text-sm font-bold text-black shadow-lg transition hover:-translate-y-1" style="background:linear-gradient(45deg, var(--main-color), var(--highlight-color));">{projectsLabel} →</a>
          <a href={cvUrl} download class="rounded-md border border-black/20 bg-black/[.03] px-6 py-3 text-sm font-bold text-black transition hover:border-black/50 hover:bg-black/5 dark:border-white/30 dark:bg-white/[.02] dark:text-white dark:hover:border-white/60 dark:hover:bg-white/5">{cvLabel} ↓</a>
        </div>
        <div class="mt-7 flex flex-wrap justify-center gap-x-7 gap-y-3 text-xs text-slate-600 dark:text-slate-400 lg:justify-start">
          <Links showText />
        </div>
    </div>
    <div class="relative mx-auto w-full min-w-0 max-w-[620px]"><Hexagon {...hexagon} /></div>
  </div>
</section>
<script>
    let clearHeroLight: (() => void) | undefined;

    const initializeHeroLight = () => {
        const hero = document.getElementById("hero");
        const light = document.getElementById("light");

        if (!hero || !light || hero.dataset.lightInitialized === "true") return;

        hero.dataset.lightInitialized = "true";

        const initialRect = hero.getBoundingClientRect();
        let currentX = initialRect.width / 2;
        let currentY = initialRect.height / 2;
        let pointerX = currentX;
        let pointerY = currentY;
        let isPointerMoving = false;
        let pointerIdleTimeout: number | undefined;
        let animationFrameId = 0;
        let angle = 0;
        let orbitOffsetX = 0;
        let orbitOffsetY = 0;
        const abortController = new AbortController();

        light.style.left = `${currentX}px`;
        light.style.top = `${currentY}px`;

        const getAutomaticPosition = () => {
            const rect = hero.getBoundingClientRect();

            return {
                x: Math.cos(angle) * (rect.width / 3) + rect.width / 2,
                y: Math.sin(angle) * (rect.height / 3) + rect.height / 2.1,
            };
        };

        const resumeAutomaticFromCurrentPosition = () => {
            const automaticPosition = getAutomaticPosition();

            orbitOffsetX = currentX - automaticPosition.x;
            orbitOffsetY = currentY - automaticPosition.y;
            isPointerMoving = false;
        };

        const renderLight = () => {
            angle += 0.01;

            const automaticPosition = getAutomaticPosition();
            const automaticX = automaticPosition.x + orbitOffsetX;
            const automaticY = automaticPosition.y + orbitOffsetY;

            const targetX = isPointerMoving ? pointerX : automaticX;
            const targetY = isPointerMoving ? pointerY : automaticY;
            const easing = 0.1;

            currentX += (targetX - currentX) * easing;
            currentY += (targetY - currentY) * easing;

            light.style.left = `${currentX}px`;
            light.style.top = `${currentY}px`;

            if (!isPointerMoving) {
                orbitOffsetX *= 0.985;
                orbitOffsetY *= 0.985;
            }

            animationFrameId = window.requestAnimationFrame(renderLight);
        };

        hero.addEventListener('pointermove', (event) => {
            const rect = hero.getBoundingClientRect();
            pointerX = event.clientX - rect.left;
            pointerY = event.clientY - rect.top;
            isPointerMoving = true;

            if (pointerIdleTimeout !== undefined) {
                window.clearTimeout(pointerIdleTimeout);
            }

            pointerIdleTimeout = window.setTimeout(() => {
                resumeAutomaticFromCurrentPosition();
                pointerIdleTimeout = undefined;
            }, 200);
        }, { signal: abortController.signal });

        hero.addEventListener('pointerleave', () => {
            resumeAutomaticFromCurrentPosition();

            if (pointerIdleTimeout !== undefined) {
                window.clearTimeout(pointerIdleTimeout);
                pointerIdleTimeout = undefined;
            }
        }, { signal: abortController.signal });

        animationFrameId = window.requestAnimationFrame(renderLight);

        clearHeroLight = () => {
            abortController.abort();
            window.cancelAnimationFrame(animationFrameId);
            if (pointerIdleTimeout !== undefined) {
                window.clearTimeout(pointerIdleTimeout);
            }
            clearHeroLight = undefined;
        };
    };

    initializeHeroLight();
    document.addEventListener('astro:page-load', initializeHeroLight);
    document.addEventListener('astro:before-swap', () => clearHeroLight?.());

</script>
